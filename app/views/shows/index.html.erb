<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" type="text/css" href="/stylesheets/application.css" />
  </head>
  <body>
    <h1>Here are some shows!</h1>
    <ul style='columns: 4'>
      <% @shows.each do |show| %>
        <li><a href='/shows/<%= show.id %>'><%= show.name %></a></li>
      <% end %>
    </ul>
    <p>Don't see your show?</p>
    <p><a href='/shows/new'>Add a show!</a></p>
  </body>
</html>